// Generated by CoffeeScript 1.6.3
(function() {
  define(function(requiere, exports, module) {
    var Asteroids;
    Asteroids = (function() {
      function Asteroids(model) {
        this.model = model;
        this.x = 0;
        this.y = 0;
      }

      Asteroids.prototype.update = function(dt) {
        this.x += dt / 10;
        return this.y += dt / 10;
      };

      Asteroids.prototype.render = function(c) {
        var H, W, asteroid, image, size, _i, _len, _ref, _results;
        W = 120;
        H = 120;
        image = window.images.Asteroids;
        c.fillStyle = 'blue';
        _ref = this.model.asteroids;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          asteroid = _ref[_i];
          c.save();
          size = asteroid.radius;
          c.beginPath();
          c.arc(asteroid.position.x, asteroid.position.y, asteroid.radius, 0, Math.PI * 2);
          c.fill();
          _results.push(c.restore());
        }
        return _results;
      };

      return Asteroids;

    })();
    return Asteroids;
  });

}).call(this);
