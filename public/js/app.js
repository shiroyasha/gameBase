// Generated by CoffeeScript 1.6.3
(function() {
  define(function(require, exports, module) {
    var App, Background, Player, RenderQueue, Stars, model;
    RenderQueue = require('RenderQueue');
    Background = require('Background');
    Stars = require('Stars');
    model = require('Model');
    Player = require('Player');
    App = (function() {
      function App(c) {
        this.c = c;
        this.model = model;
        this.queue = new RenderQueue();
        this.queue.add(new Background());
        this.queue.add(new Stars(model));
        this.queue.add(new Player(model));
        this.lastTime = null;
        requestAnimationFrame(this._loop.bind(this));
      }

      App.prototype.update = function(dt) {
        return this.queue.update(dt);
      };

      App.prototype.render = function(c) {
        return this.queue.render(c);
      };

      App.prototype._loop = function(time) {
        var dt;
        if (this.lastTime === null) {
          this.lastTime = time;
        }
        dt = time - this.lastTime;
        this.update(dt);
        this.render(this.c);
        this.lastTime = time;
        return requestAnimationFrame(this._loop.bind(this));
      };

      return App;

    })();
    return App;
  });

}).call(this);
