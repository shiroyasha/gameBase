// Generated by CoffeeScript 1.6.3
(function() {
  define(function(require, exports, module) {
    var Planets;
    Planets = (function() {
      function Planets(model) {
        this.model = model;
        this.x = 0;
        this.y = 0;
      }

      Planets.prototype.update = function(dt) {
        this.x += dt / 10;
        return this.y += dt / 10;
      };

      Planets.prototype.render = function(c) {
        var H, W, image, planet, size, _i, _len, _ref, _results;
        image = window.images.Planet;
        W = image.width;
        H = image.height;
        _ref = this.model.planets;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          planet = _ref[_i];
          c.save();
          c.translate(planet.position.x + W / 2, planet.position.y + H / 2);
          c.rotate(planet.orientation);
          size = planet.radius * 2;
          c.drawImage(image, 0, 0, W, H, -size / 2, -size / 2, size, size);
          _results.push(c.restore());
        }
        return _results;
      };

      return Planets;

    })();
    return Planets;
  });

}).call(this);
